on:
  workflow_dispatch:
    input:
      environment:
        description: "Choose envenviroment"
        required: true
        default :"staging"
        type: choice
        options:
         -staging
         -production
  

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy application
        run: echo "Deploying the application..."
